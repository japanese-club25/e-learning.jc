import { Eye, Trash2 } from "lucide-react";
import { Student } from "./types";
interface Props { students: Student[]; selectedStudents: string[]; isDeleting: string | null; onSelectAll: () => void; onSelectStudent: (id: string) => void; onViewDetail: (student: Student) => void; onDelete: (id: string, name: string) => void; }
export function StudentCardView({ students, selectedStudents, isDeleting, onSelectAll, onSelectStudent, onViewDetail, onDelete }: Props) { return <div className="p-4"><div className="mb-4 flex items-center gap-3"><input type="checkbox" checked={students.length > 0 && students.every((s) => selectedStudents.includes(s.id))} onChange={onSelectAll} /><span className="text-sm text-slate-600">Select all</span></div><div className="grid gap-4 md:grid-cols-2">{students.map((student) => <article key={student.id} className="rounded-xl border border-orange-100 bg-white p-5"><div className="flex items-center gap-3"><input type="checkbox" checked={selectedStudents.includes(student.id)} onChange={() => onSelectStudent(student.id)} /><div><h3 className="font-semibold text-slate-900">{student.name}</h3><p className="text-sm text-slate-500">{student.email || "No email"}</p></div></div><dl className="mt-4 space-y-2 text-sm"><div className="flex justify-between"><dt className="text-slate-500">Class</dt><dd className="font-medium text-slate-800">{student.class}</dd></div><div className="flex justify-between"><dt className="text-slate-500">Account</dt><dd className="font-medium text-orange-700">{student.is_first_login ? "First Login" : "Active"}</dd></div></dl><div className="mt-4 flex gap-2"><button onClick={() => onViewDetail(student)} className="flex-1 rounded-lg border border-orange-200 px-3 py-2 text-sm font-semibold text-orange-700"><Eye className="mr-1 inline h-4 w-4" />Detail</button><button disabled={isDeleting === student.id} onClick={() => onDelete(student.id, student.name)} className="rounded-lg border border-red-200 px-3 py-2 text-red-600"><Trash2 className="h-4 w-4" /></button></div></article>)}</div></div>; }
